# Fork feature contract

Downstream inventory for the `ViksCO/emdash` fork of `generalaction/emdash`. Every custom behavior the fork carries lives in one row here. **When syncing upstream, walk this table and verify each row is still present in the merged tree.** A cleanly-deleted event handler leaves no git conflict — only an explicit checklist catches that.

Each row's outcome after a sync is one of:
- **carried** — still present, behavior verified
- **retired** — deliberately dropped (record why)
- **upstreamed** — merged upstream, no longer needed in fork
- **reimplemented** — behavior kept but code shape had to change against new upstream

Never let a row silently disappear.

**Sync outcomes:** the rightmost column records the result of each upstream sync (see [Sync procedure](#sync-procedure)). An outcome is `carried` / `reimplemented` / `retired` / `upstreamed`; code-level outcomes are recorded when the merge resolves, and runtime smoke (the Manual-smoke column) is re-run before `main` is advanced.

| ID | Commit | Behavior | Manual smoke | Automated check | 2026-08 sync (→ `17869bfbd`) |
|---|---|---|---|---|---|
| ADE-0 | `e19991f22` | Selectable WebStorm New UI dark theme in Settings → Color mode. `isDarkTheme()` treats WebStorm as dark alongside `emdark`. | Settings → Color mode → WebStorm New UI → app repaints; terminals, editor accents, selection look right. | none | **reimplemented** — upstream refactored `AgentIcon`→`PluginIcon`; the `isDarkTheme()` check moved into `lib/components/plugin-icon.tsx` (its default `=== 'emdark'` would have dropped WebStorm-as-dark). |
| ADE-1 | `63886a131` | Diff toolbar shows `filename` + `directory` + Copy-path button; changes-list rows show a truncation tooltip when the path overflows; `TooltipContent` is `pointer-events-none` project-wide. | Open a diff → toolbar shows file path with copy button (click → checkmark); hover a long path in the changes panel → tooltip appears with the full path. | none | **carried** — surviving files; adapted the upstream `DiffTabStore`→`DiffTabResource` rename and the `rootPath`/`displayPath` change in changes-list. |
| ADE-2 | `008b22c2f` | Vertical mouse-wheel scrolls the task tab bar horizontally when it overflows (imperative `wheel` listener with `passive:false`, `deltaY !== 0 && deltaX === 0`). | Open many tabs so the strip overflows → plain-mouse vertical wheel scrolls the tab strip left/right; trackpad horizontal swipe still works unchanged. | none | **reimplemented** — the tab bar moved to a generic `features/tabs/tab-bar.tsx`; the wheel `useEffect` was ported onto its existing `scrollContainerRef`. |
| ADE-3 | `81e1c7725` | Hover-only ↗ button on sidebar conversation rows to open the conversation in a persistent (non-preview) tab; skips the auto-rename side effect of the existing double-click path. | Hover a conversation row → arrow icon appears at the right → click → conversation opens in a stable tab (non-italic), no rename triggered. | none | **reimplemented** — the old `tabManager`/`tabGroupManager` API is gone; the button + middle-click now call `useTabSelection().open({conversationId},{preview:false})`. |
| ADE-4 | `ef49a80da` | Read jail widened to `homedir + os.tmpdir() + /tmp + /private/tmp` (`resolveJailedPath` + `assertWithinJail` in `main/core/app/service.ts`, shared by `openPath`, `readUserFile`, `readAudioFileDataUrl`). External file tabs render with a `FileSymlink` icon (`tasks/editor/file-tab-item.tsx`). | ⌘K a temp-dir file (e.g. `/tmp/foo.md`) → opens; tab shows the symlink icon; `/etc/passwd` still blocked. | jail tests in `service.test.ts` | **reimplemented** — jail containment now uses upstream's `contains()` (its `isPathInsideRoot` helper was deleted); `resolveHomeJailedPath` restored so upstream's new home-only `showItemInFolder` keeps its policy; symlink icon ported into the new `tasks/editor/file-tab-item.tsx`. The stuck-spinner skip is now **upstreamed** (`isFileTabLoading` accounts for `isExternal`). |
| ADE-5 | `1a7ba4fad` | Preview scroll offset persists across tab switches within a session (markdown/svg, keyed by tab store + path — dies with the tab, **not** across restart). Monaco source view state (scroll + cursor) persists per model URI and **also survives app restarts**. | Open `.md`, scroll to bottom, switch tab, return → still at bottom (within the session). In source (edit) mode: scroll + cursor survive a tab switch and an app restart. | none | **reimplemented** — upstream now holds Monaco view-state in memory (`attach`/`detach`); the distinctive restart-persistence (`editorViewStatePersistence`) + continuous-save timing were grafted on, the preview hook kept in the surviving markdown/svg renderers, the old renderers accepted-deleted, and the dead `triggerLayout`/`restoreActiveViewState` dropped (their only caller was a deleted renderer). Contract wording corrected: preview is session-scoped, not restart-durable. |
| ADE-6 | `8c148f3ec` | Sidebar conversations panel visually marks rows that already have a tab open (background tabs, not just the currently-focused one). Three states: focused / open-in-background / not-open. | Open 3 conversation tabs, switch to a fourth surface (say a file) → in the sidebar, the 3 conversation rows are visually distinguished as "in a background tab" separately from the currently-focused row (if any). | none | **reimplemented** — `hasTab` now derives from `useTabSelection().isOpen`; the mark span was kept. |
| ADE-8 | `58934ddcd` | ⌘K → file result → opens the file in a read-only peek modal (in-place cross-fade under the same scrim, no page nav). Uses Monaco's `colorize()` for highlighting; `.md` renders via the existing markdown renderer; images/binaries handled correctly. | ⌘K → type a filename → select a file result → peek modal opens with syntax-highlighted content; Escape closes; focus returns to the palette. | none | **reimplemented** — the `bare` full-bleed modal option + the `cmdk-stage` search/peek layers were carried into the rewritten palette + modal renderer; `code-colorizer` moved off the deleted `monaco-code-pool` onto `monaco-bootstrap`. |
| ADE-9 | `0f141f8bf` | ⌘K palette finds files in the current project's local repo (and, via `@`, any discovered git repo under the dev root) — not just db-indexed sources — and peeks them (see ADE-8). | ⌘K → type a filename that lives only in the current project's repo (not workspace-indexed) → the file appears → select → peek opens. `@` → pick another repo → find + peek its files. | none | **reimplemented** — `listRepoFiles` re-pointed off the deleted `local-fs` onto `git ls-files -co --exclude-standard` (native `.gitignore`, bounded, cancellable); repo discovery + the controller RPC kept; the `@` picker + peek carried into the rewritten palette. NB: upstream's own palette file search is workspace-index-scoped, so it does **not** subsume this (base repo without an active workspace, cross-repo). |
| ADE-10 | `36f7fd4f5` | `resolveUserEnv` strips the app checkout's own `node_modules/.bin` dirs from `process.env.PATH` (scoped via `app.getAppPath()`), so a dev-launcher (pnpm/nx) workspace shim never shadows the user's global CLI on spawned agents/terminals — e.g. a `node_modules/.bin/codex` shim (pulled in via `codex-acp`) installed without its platform-native binary no longer wins over the global codex. | In dev: a spawned agent/terminal's `which -a codex` resolves the global (nvm/homebrew), not `…/emdash/node_modules/.bin/codex`, and `codex --version` runs; an unrelated project's own `./node_modules/.bin` still resolves in its own terminal. | `childProcessEnv.test.ts` — `stripDevWorkspaceBinDirs` | n/a — added after this sync |

## Sync procedure

1. **Read this file.** For each row above, know what "carried" looks like before the merge.
2. **Merge upstream in a worktree.** Never in the primary checkout while `pnpm dev` is running.
3. **Resolve conflicts** aiming to keep every row above `carried`. If upstream restructured code a row depended on, port the behavior to the new location and record the outcome as `reimplemented`.
4. **Two-sided deletion audit — the silent-loss vectors conflict markers don't show.** Walking the table catches feature-level loss; these two catch break-level loss that merges cleanly and even passes typecheck:
   - **Modify/delete conflicts** — `git diff --name-only --diff-filter=U`, then `git ls-files -u <path>` shows stages 1+2 only (no stage 3): upstream deleted a file the fork had patched. Port the behavior to its new home and mark `reimplemented` — don't keep the orphan (its callers/imports dangle) or drop it silently (lost feature). The 2026-08 sync hit this on `tab-bar`, the file renderers, and `local-fs`.
   - **Dead imports in surviving fork-only files** — a fork-only file with no conflict marker can still `import` a module upstream deleted; nothing flags it and a green typecheck can miss it. Grep surviving fork files for imports of now-deleted paths. The 2026-08 sync hit `code-colorizer`→`monaco-code-pool`, `svg-renderer`'s stale `FileTabStore`, and a dangling `resolveHomeJailedPath` / `isPathInsideRoot`.
5. **Verify with the full local suite, then runtime-smoke against a copy** — CI and typecheck alone aren't enough:
   - Run `pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint && pnpm -w build` plus the affected unit tests. Typecheck skips unused imports (oxlint catches them); CI skips the full build. Beware masked exit codes — `nx … | tail` reports `tail`'s status, not nx's.
   - Then smoke every row with real data at zero risk: **stop dev, back up `~/Library/Application Support/emdash-dev`, and launch against a copy** via `EMDASH_DB_FILE=<copy-of-emdash4.db>` (dev mode uses the `emdash-dev` userData dir; the flag redirects only the DB, so the real one is never migrated or opened).
6. **Preserve the merge commit's ancestry.** Never squash the upstream sync merge — that flattens the parent link that keeps the *next* sync small. Fast-forward or merge into `main`; don't `reset --hard`.
7. **Record outcomes.** After the sync, update the "outcome" column for the release you synced to (rows are `carried` by default; anything else needs an entry).

## Recovery: `git rerere`

`git config rerere.enabled true` at the fork level. Conflict resolutions get recorded and offered back on the next similar conflict — useful for repeated lockfile churn, imports, and identical patches that keep re-conflicting. `autoUpdate` intentionally left `false`: rerere writes the suggested resolution to the working tree but leaves it unstaged, so you still review before committing.

## Housekeeping

- Every new ADE-N feature gets a row here in the same PR that introduces it.
- Removed features (`retired` / `upstreamed`) stay in the table with the outcome recorded, don't delete rows.
- Keep this file at repo root so a `git status`-only reader can see it exists.
